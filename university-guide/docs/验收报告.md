# 实施验收报告

验收日期：2026-08-11。

## 结论

核心发布包已实施并通过数据、生成、严格构建、内部链接与内容抽查门槛。外部链接中 110 条有当日自动 HTTP 通过记录，2 条重定向循环已换成官方当前入口并人工打开核验。真实窄屏浏览器点击/截图因本执行环境禁止本地页访问而未伪装为通过；离线响应式结构检查已通过，发布前应补一次可视回归。

## 交付覆盖

| 验收点 | 结果 | 证据 |
|---|---|---|
| 56 个能力点 | 通过 | 32 公共 + 24 专业；每项含先修、成果、学时、产物和量化验收 |
| 112 项视频 | 通过 | 56 主 + 56 备选；章节、语言、字幕、时长、练习、分项评分、日期完整 |
| 依赖与建议窗口 | 通过 | 无缺失引用、无环；五个窗口仅表达内容关系，可以按兴趣提前、延后或跳过 |
| 自由探索框架 | 通过 | 无学期分配、年级期限或学期总学时门槛；提供兴趣优先、项目优先、基础优先三种入口 |
| 参考投入 | 通过 | 每个能力点保留参考学时和建议章节；不汇总成学期配额，也不要求按固定视频比例学习 |
| 六路线/17 方向 | 通过 | 17 方向不重不漏；每路线恰有 4 个专业能力 |
| 课程—能力—路线映射 | 通过 | 56 条映射全覆盖；full 29、partial 21、gap 6；缺口未伪造课程 |
| 六路线内容抽查 | 通过 | 每路线 2 项，共 12 项，对照官方逐讲/模块证据；12 PASS、0 FAIL |
| 严格构建 | 通过 | MkDocs 1.6.1 + Material 9.7.7，`mkdocs build --strict` 返回 0 |
| 内链与响应式结构 | 通过 | 75 HTML、所有内部链接/锚点、viewport、移动 CSS、112 行与 7 类筛选通过离线检查 |
| 外部链接 | 有记录的人工收口 | 110 自动 `ok` + 2 官方当前页 `manual_review_ok`；发布环境应最终重跑严格检查 |
| 真实窄屏视觉/点击 | 环境受限 | 未伪装为通过；复现步骤见 `reports/interface-qa.md` |

## 可复现命令

```bash
python scripts/validate.py --strict
python scripts/render.py
python scripts/render.py --check
mkdocs build --strict
python scripts/check_site.py
python scripts/check_links.py --strict --report reports/link-check.json
```

## 发布前两个环境依赖动作

1. 在可联网的本地/CI 环境重跑外链严格检查，使当前两个人工验证项也取得自动记录。
2. 运行 `mkdocs serve`，在 390×844 和 768×1024 视口各做一次首页、能力页、路线页和资源组合筛选的可视回归。

上述两项是发布环境验收，不影响数据、站点源码、静态构建和审计改进包的交付。
